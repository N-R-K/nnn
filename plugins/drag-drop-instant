#!/bin/sh

selection=${NNN_SEL:-${XDG_CONFIG_HOME:-$HOME/.config}/nnn/.selection}
args="-a -x -T"

if [ -s "$selection" ]; then
  sed -z 's|'"$PWD/"'||g;' < "$selection" | xargs -0 setsid -f dragon ${args}
else
  setsid -f dragon ${args} "$1"
fi

[ $? -eq 0 ] && printf "-" > "$NNN_PIPE"
