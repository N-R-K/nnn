#!/bin/sh

selection=${NNN_SEL:-${XDG_CONFIG_HOME:-$HOME/.config}/nnn/.selection}
dnd="dragon"

[ -s "$selection" ] && \
    sed -z 's|'"$PWD/"'||g' < "$selection" | xargs -0 setsid -f "$dnd" -a -x -T && \
    rm "$selection" || \
    setsid -f "$dnd" -x -T "$1"
